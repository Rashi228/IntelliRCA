FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt requirements-ai.txt ./
RUN pip install -r requirements.txt -r requirements-ai.txt
COPY . .
ENV PYTHONPATH=/app
CMD ["uvicorn", "services.incident_memory.api:app", "--host", "0.0.0.0", "--port", "8087"]
